import { useMemo } from 'react';
import * as THREE from 'three';
export default function ParticleField({ count = 64 }) { const positions = useMemo(() => { const values = new Float32Array(count * 3); for (let index = 0; index < count; index += 1) { const radius = 2.9 + Math.random() * 3.1; const theta = Math.random() * Math.PI * 2; const phi = Math.acos(2 * Math.random() - 1); values[index * 3] = radius * Math.sin(phi) * Math.cos(theta); values[index * 3 + 1] = radius * Math.cos(phi) * 0.72; values[index * 3 + 2] = radius * Math.sin(phi) * Math.sin(theta); } return values; }, [count]); return <points><bufferGeometry><bufferAttribute attach="attributes-position" count={count} array={positions} itemSize={3} /></bufferGeometry><pointsMaterial color="#8fe6f7" size={0.018} sizeAttenuation transparent opacity={0.52} depthWrite={false} /></points>; }
