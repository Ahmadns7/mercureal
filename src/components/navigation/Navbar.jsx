import { useEffect, useState } from 'react';
import { Menu, Moon, Sun, X, ArrowUpRight } from 'lucide-react';
const links=['Home','Services','Solutions','Projects','About','Contact'];
export default function Navbar({theme,setTheme}){const[open,setOpen]=useState(false);const[scrolled,setScrolled]=useState(false);useEffect(()=>{const f=()=>setScrolled(scrollY>12);f();addEventListener('scroll',f);return()=>removeEventListener('scroll',f)},[]);return <header className={`nav ${scrolled?'nav-scrolled':''}`}><a className="brand" href="#home" aria-label="MercuReal home"><span className="brand-mark">M</span><span>MercuReal <em>Tech Solutions</em></span></a><nav aria-label="Main navigation">{links.map(link=><a href={`#${link.toLowerCase()}`} key={link}>{link}</a>)}</nav><div className="nav-actions"><button className="icon-button" aria-label="Toggle color theme" onClick={()=>setTheme(theme==='dark'?'light':'dark')}>{theme==='dark'?<Sun size={17}/>:<Moon size={17}/>}</button><a className="button button-small" href="#contact">Start a Project <ArrowUpRight size={15}/></a><button className="icon-button menu-toggle" aria-label="Open navigation menu" aria-expanded={open} onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<div className="mobile-menu">{links.map(link=><a onClick={()=>setOpen(false)} href={`#${link.toLowerCase()}`} key={link}>{link}</a>)}<a className="button" href="#contact">Start a Project <ArrowUpRight size={15}/></a></div>}</header>}
